package com.pds.skillify.ui;
/*
import com.pds.skillify.model.User;
import org.junit.jupiter.api.Test;
import org.netbeans.jemmy.operators.JFrameOperator;
import org.netbeans.jemmy.operators.JLabelOperator;

import static org.junit.jupiter.api.Assertions.*;

public class MainWindowTest {

    @Test
    public void testMainWindowLoadsAndComponentsArePresent() {
        User testUser = new User("testUser", "pass", "email@test.com", null);
        MainWindow mainWindow = new MainWindow();
        JFrameOperator frame = new JFrameOperator("Skillify");

        JLabelOperator label = new JLabelOperator(frame, 0);
        String labelText = label.getText();
        assertTrue(labelText.contains("testUser"), "No contiene el nombre de usuario");
    }
}
*/