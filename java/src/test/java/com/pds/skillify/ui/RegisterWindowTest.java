package com.pds.skillify.ui;
/*
import org.junit.jupiter.api.Test;
import org.netbeans.jemmy.operators.JFrameOperator;
import org.netbeans.jemmy.operators.JTextFieldOperator;

import static org.junit.jupiter.api.Assertions.*;

public class RegisterWindowTest {

    @Test
    public void testRegisterWindowComponents() {
        RegisterWindow window = new RegisterWindow();
        JFrameOperator frame = new JFrameOperator("Skillify");

        JTextFieldOperator field = new JTextFieldOperator(frame, 0);
        assertNotNull(field);
    }
}
*/